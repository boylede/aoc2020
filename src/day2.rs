use crate::{PartError, PartResult};

pub fn part1(lines: &Vec<String>) -> PartResult {
    let result = 0;
    // code here

    Err(PartError::Failed("test of failure message".to_string()))
}

pub fn part2(lines: &Vec<String>) -> PartResult {
    let result = 0;
    // code here

    Err(PartError::Unimplemented)
}
